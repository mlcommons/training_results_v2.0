/*
 * These provide and require statements are not required, but using them is
 * very important. They document the relationship between stylesheets and allow
 * the build system to perform strict dependency checking.
 */
@provide 'closure.rules.bar';
@require 'closure.rules.foo';

.misery {
  color: COLOR;
}
